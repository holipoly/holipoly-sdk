import { hookFactory } from "../helpers/hookFactory";

/**
 * React hook to get client's config methods
 *
 * @returns Holipoly's client's config methods
 */
export const useHolipolyConfig = hookFactory("config");
